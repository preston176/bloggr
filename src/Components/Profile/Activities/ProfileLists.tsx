import { UserDetails } from "../../../Context/Context";

const ProfileLists = ({ getUserData, setEditModal }: {
    getUserData: UserDetails | undefined;
    setEditModal: React.Dispatch<React.SetStateAction<boolean>>;
}) => {
    return (
        <div>
            ProfileLists
        </div>
    )
}

export default ProfileLists
